PORT=4000
SQLITE_PATH=data/occibo.sqlite
REDIS_URL=redis://127.0.0.1:6379
PUBLIC_BASE_URL=http://localhost:4000
CORS_ORIGIN=http://localhost:3000
# Optional. When set, RealImageProvider is used (Pollinations public API by default).
# Leave empty to use MockImageProvider (SVG placeholder with the prompt on it).
IMAGE_API_KEY=
# Optional Hugging Face-style inference endpoint. Requires IMAGE_API_KEY.
# IMAGE_API_URL=https://router.huggingface.co/hf-inference/models/black-forest-labs/FLUX.1-schnell
